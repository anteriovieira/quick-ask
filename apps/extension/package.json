{
    "name": "quickask-extension",
    "version": "1.1.4",
    "private": true,
    "description": "Chrome extension for QuickAsk",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "package": "zip -r extension.zip . -x \"*.git*\" -x \"node_modules/*\" -x \".DS_Store\"",
        "bump": "node scripts/bump-version.js"
    }
}